---
name: project
description: Design and run applied statistics/data science projects and Python/R experiments on real datasets for the MIT Learning OS, with all computation on the remote target. Use for /project, "run an experiment", "simulate", "apply this to data".
---

# /project

Mode (entry point). Argument: idea, concept to test, or existing project/experiment.

1. Frame the question and connect it to concept/formula notes.
2. Run `experiment-manager` (spec, environment, remote run when bound and approved).
3. Interpret against the prediction; hand misconceptions to `error-analyzer`.
4. Multi-experiment efforts also get a `project` note in `70-Projects/`.
