---
name: evolve
description: Propose and implement changes to the MIT Learning OS architecture (folders, templates, schema, modes, compute binding, rules) through the approval gate. Use for /evolve, "add a mode", "change the structure", "bind the compute target".
---

# /evolve

Mode (entry point). Argument: the desired change or problem.

Run `vault-architect`: propose, wait for explicit approval, implement the approved scope, update the docs it lists, validate, report. No commit or push without approval.
